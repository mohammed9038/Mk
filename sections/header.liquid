<link rel="stylesheet" href="{{ 'component-list-menu.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-search.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-search-bar.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-menu-drawer.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-cart-notification.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-cart-items.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-mobile-navigation.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'search-icon-fix.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'mobile-nav-emergency-fix.css' | asset_url }}" media="print" onload="this.media='all'">
{%- if settings.predictive_search_enabled -%}
  <link rel="stylesheet" href="{{ 'component-price.css' | asset_url }}" media="print" onload="this.media='all'">
{%- endif -%}
{%- if section.settings.menu_type_desktop == 'mega' -%}
  <link rel="stylesheet" href="{{ 'component-mega-menu.css' | asset_url }}" media="print" onload="this.media='all'">
  <noscript>{{ 'component-mega-menu.css' | asset_url | stylesheet_tag }}</noscript>
{%- endif -%}

{%- if settings.cart_type == "drawer" -%}
  {{ 'component-cart-drawer.css' | asset_url | stylesheet_tag }}
  {{ 'component-cart.css' | asset_url | stylesheet_tag }}
  {{ 'component-totals.css' | asset_url | stylesheet_tag }}
  {{ 'component-price.css' | asset_url | stylesheet_tag }}
  {{ 'component-discounts.css' | asset_url | stylesheet_tag }}
{%- endif -%}

<noscript>{{ 'component-list-menu.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'component-search.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'component-search-bar.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'component-menu-drawer.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'component-cart-notification.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'component-cart-items.css' | asset_url | stylesheet_tag }}</noscript>
{%- if section.settings.enable_mobile_navigation -%}
  <noscript>{{ 'component-mobile-navigation.css' | asset_url | stylesheet_tag }}</noscript>
{%- endif -%}

<style>
  header-drawer {
    justify-self: start;
    margin-left: -1.2rem;
  }

  {%- if section.settings.sticky_header_type == 'reduce-logo-size' -%}
    .scrolled-past-header .header__heading-logo-wrapper {
      width: 75%;
    }
  {%- endif -%}

  {%- if section.settings.menu_type_desktop != "drawer" -%}
    @media screen and (min-width: 990px) {
      header-drawer {
        display: none;
      }
    }
  {%- endif -%}

  .menu-drawer-container {
    display: flex;
  }

  /* Top Navigation Styles */
  .top-nav-wrapper {
    border-bottom: 1px solid rgba(var(--color-foreground), 0.08);
    padding: 0.5rem 0;
  }

  .top-nav__list {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .top-nav__link {
    font-size: 1.3rem;
    text-decoration: none;
    color: rgba(var(--color-foreground), 0.7);
    transition: color var(--duration-short) ease;
  }

  .top-nav__link:hover,
  .top-nav__link--active {
    color: rgba(var(--color-foreground), 1);
  }

  @media screen and (max-width: 749px) {
    .top-nav-wrapper {
      display: none;
    }
  }

  /* Language selector positioning */
  .header__left-localization {
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
  }

  .list-menu {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .list-menu--inline {
    display: inline-flex;
    flex-wrap: wrap;
  }

  summary.list-menu__item {
    padding-right: 2.7rem;
  }

  .list-menu__item {
    display: flex;
    align-items: center;
    line-height: calc(1 + 0.3 / var(--font-body-scale));
  }

  .list-menu__item--link {
    text-decoration: none;
    padding-bottom: 1rem;
    padding-top: 1rem;
    line-height: calc(1 + 0.8 / var(--font-body-scale));
  }

  @media screen and (min-width: 750px) {
    .list-menu__item--link {
      padding-bottom: 0.5rem;
      padding-top: 0.5rem;
    }
  }
</style>

{%- style -%}
  .header {
    padding: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px 3rem {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px 3rem;
  }

  .section-header {
    position: sticky; /* This is for fixing a Safari z-index issue. PR #2147 */
    margin-bottom: {{ section.settings.margin_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-header {
      margin-bottom: {{ section.settings.margin_bottom }}px;
    }
  }

  @media screen and (min-width: 990px) {
    .header {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  /* Search positioning styles */
  .header__search--left {
    order: 1;
    margin-right: 1rem;
  }
  
  .header__search--center {
    display: flex;
    justify-content: center;
    flex: 1;
    margin: 0 1rem;
  }
  
  @media screen and (max-width: 749px) {
    .header__search--left,
    .header__search--center {
      display: none;
    }
  }

  /* Header Layout Styles */
  .header__top-nav {
    border-bottom: 1px solid rgba(var(--color-foreground), 0.08);
    padding: 0.5rem 0;
    text-align: center;
  }

  .header__top-navigation .list-menu {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .header__main-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    gap: 1rem;
  }

  .header__left-section {
    display: flex;
    align-items: center;
    flex: 0 0 auto;
  }

  .header__center-section {
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 1 1 auto;
    max-width: 500px;
    margin: 0 2rem;
  }

  .header__right-section {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex: 0 0 auto;
  }

  .header__search-bar {
    width: 100%;
    max-width: 400px;
  }

  .header__search--mobile-only {
    display: none;
  }

  .header__localization {
    display: flex;
    align-items: center;
  }

  /* Desktop: Hide mobile search when search bar is shown */
  @media screen and (min-width: 990px) {
    .header__search--mobile-only {
      display: none !important;
    }
  }

  /* Mobile responsive adjustments */
  @media screen and (max-width: 989px) {
    .header__top-nav {
      display: none;
    }
    
    .header__center-section {
      display: none;
    }
    
    .header__search-bar {
      display: none;
    }

    .header__search--mobile-only {
      display: block;
    }

    .header__main-row {
      justify-content: space-between;
    }
  }

  @media screen and (max-width: 749px) {
    .header__right-section .header__localization {
      display: none;
    }
  }

  /* ========================================
     DYNAMIC SEARCH BAR STYLING - Uses Admin Settings
     This CSS is generated from your section settings
     Fixed selectors to match actual HTML structure
     ======================================== */
  
  /* Search form container */
  /* Search form container - unified targeting */
  .header__search-form,
  .header__search-form__form,
  .header__search-bar .header__search-form {
    width: 100%;
    max-width: {{ section.settings.search_width | default: 400 }}px;
    position: relative;
    display: flex;
    align-items: center;
  }

  /* Search input - improved targeting with priority */
  .header__search-form .search__input.field__input,
  .header__search-form input[type="search"],
  .header__search-form .header__search-input,
  .header__search-bar input.search__input,
  .header__search-bar .field__input {
    /* Size and spacing */
    width: 100% !important;
    height: 45px !important;
    padding: 0 50px 0 20px !important;
    margin: 0 !important;
    
    /* Colors from admin settings */
    background-color: {{ section.settings.search_background | default: '#ffffff' }} !important;
    color: {{ section.settings.search_text | default: '#333333' }} !important;
    border: 2px solid {{ section.settings.search_border | default: '#e0e0e0' }} !important;
    
    /* Visual design */
    border-radius: 25px !important;
    font-size: 14px !important;
    font-weight: 400 !important;
    transition: all 0.3s ease !important;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1) !important;
    backdrop-filter: blur(10px) !important;
    
    /* Override theme defaults */
    -webkit-appearance: none !important;
    appearance: none !important;
    position: relative !important;
    top: auto !important;
    left: auto !important;
    right: auto !important;
    bottom: auto !important;
    flex-grow: 1 !important;
    text-align: left !important;
  }

  /* Focus state with admin colors - improved targeting */
  .header__search-form .search__input.field__input:focus,
  .header__search-form input[type="search"]:focus,
  .header__search-form .header__search-input:focus,
  .header__search-bar input.search__input:focus,
  .header__search-bar .field__input:focus {
    background-color: {{ section.settings.search_background | default: '#ffffff' }} !important;
    border-color: {{ section.settings.search_border | default: '#e0e0e0' }} !important;
    box-shadow: 0 0 0 3px {{ section.settings.search_border | default: '#e0e0e0' | color_modify: 'alpha', 0.3 }} !important;
    outline: none !important;
  }

  /* Placeholder styling - improved targeting */
  .header__search-form .search__input.field__input::placeholder,
  .header__search-form input[type="search"]::placeholder,
  .header__search-form .header__search-input::placeholder,
  .header__search-bar input.search__input::placeholder,
  .header__search-bar .field__input::placeholder {
    color: {{ section.settings.search_text | default: '#333333' | color_modify: 'alpha', 0.6 }} !important;
    font-style: normal !important;
    opacity: 1 !important;
  }

  /* Search button styling - improved targeting */
  .header__search-form .search__button.field__button,
  .header__search-form .header__search-button,
  .header__search-bar .search__button,
  .header__search-bar .field__button {
    position: absolute !important;
    right: 8px !important;
    top: 50% !important;
    transform: translateY(-50%) !important;
    background: {{ section.settings.search_border | default: '#e0e0e0' }} !important;
    color: {{ section.settings.search_text | default: '#333333' }} !important;
    border: none !important;
    border-radius: 50% !important;
    width: 32px !important;
    height: 32px !important;
    padding: 6px !important;
    cursor: pointer !important;
    transition: all 0.3s ease !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    z-index: 2 !important;
  }

  /* Search button hover */
  .header__search-form .search__button.field__button.header__search-button:hover,
  .header__search-form .header__search-button:hover {
    background: {{ section.settings.search_border | default: '#e0e0e0' | color_modify: 'brightness', -0.1 }} !important;
    transform: translateY(-50%) scale(1.05) !important;
  }

  /* Search icon styling */
  .header__search-form .search__button.field__button .icon,
  .header__search-form .header__search-button .icon,
  .header__search-form .search__button.field__button svg,
  .header__search-form .header__search-button svg {
    width: 16px !important;
    height: 16px !important;
    fill: currentColor !important;
  }

  /* Hide search field label */
  .header__search-form .field__label {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* Override any conflicting component styles */
  .header__search-form input.search__input.field__input {
    padding-right: 50px !important; /* Override component-search.css */
  }

  /* Ensure proper positioning for the search form */
  .header__search--left .header__search-form,
  .header__search--center .header__search-form,
  .header__search--right .header__search-form {
    max-width: {{ section.settings.search_width | default: 400 }}px;
  }
  .header__search-form .header__search-field:before,
  .header__search-form .header__search-field:after {
    display: none !important;
  }

  /* Responsive width */
  @media screen and (max-width: 990px) {
    .header__search-form {
      max-width: {{ section.settings.search_width | default: 400 | divided_by: 1.3 }}px;
    }
  }

  @media screen and (max-width: 749px) {
    .header__search-form {
      max-width: {{ section.settings.search_width | default: 400 | divided_by: 1.6 }}px;
    }
  }

  /* ========================================
     MOBILE NAVIGATION DYNAMIC STYLING
     Uses theme customizer settings - FIXED VERSION
     ======================================== */
  
  {% if section.settings.enable_mobile_navigation %}
    .mobile-nav {
      display: none !important; /* Hidden by default, shown on mobile */
      
      {% case section.settings.mobile_nav_style %}
        {% when 'ios' %}
          background: rgba(255, 255, 255, {{ section.settings.mobile_nav_transparency | default: 90 | divided_by: 100.0 }}) !important;
          backdrop-filter: blur(20px) !important;
          border-top: 1px solid rgba(0, 0, 0, 0.1) !important;
        {% when 'android' %}
          background: rgba(255, 255, 255, {{ section.settings.mobile_nav_transparency | default: 95 | divided_by: 100.0 }}) !important;
          box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.15) !important;
          border-top: none !important;
          border-radius: 0 !important;
        {% when 'dark' %}
          background: rgba(26, 26, 26, {{ section.settings.mobile_nav_transparency | default: 90 | divided_by: 100.0 }}) !important;
          border-top: 1px solid #333 !important;
        {% when 'gradient' %}
          background: linear-gradient(135deg, 
            rgba(102, 126, 234, {{ section.settings.mobile_nav_transparency | default: 90 | divided_by: 100.0 }}), 
            rgba(118, 75, 162, {{ section.settings.mobile_nav_transparency | default: 90 | divided_by: 100.0 }})) !important;
          border-top: none !important;
        {% when 'custom' %}
          {% assign bg_color = section.settings.mobile_nav_background_color | default: '#FFFFFF' %}
          {% assign transparency = section.settings.mobile_nav_transparency | default: 95 | divided_by: 100.0 %}
          background: {{ bg_color | color_modify: 'alpha', transparency }} !important;
          border-top: 1px solid {{ section.settings.mobile_nav_text_color | default: '#E5E5E5' | color_modify: 'alpha', 0.3 }} !important;
        {% else %}
          background: rgba(var(--color-background), {{ section.settings.mobile_nav_transparency | default: 95 | divided_by: 100.0 }}) !important;
          border-top: 1px solid rgba(var(--color-border), 0.2) !important;
      {% endcase %}
      
      {% case section.settings.mobile_nav_height %}
        {% when 'compact' %}
          padding: 6px 0 calc(6px + env(safe-area-inset-bottom)) 0 !important;
          min-height: 40px !important;
        {% when 'tall' %}
          padding: 16px 0 calc(16px + env(safe-area-inset-bottom)) 0 !important;
          min-height: 72px !important;
        {% else %}
          padding: 10px 0 calc(10px + env(safe-area-inset-bottom)) 0 !important;
          min-height: 56px !important;
      {% endcase %}
      
      border-radius: {{ section.settings.mobile_nav_border_radius | default: 0 }}px {{ section.settings.mobile_nav_border_radius | default: 0 }}px 0 0 !important;
    }
    
    .mobile-nav__link {
      {% case section.settings.mobile_nav_style %}
        {% when 'ios' %}
          color: #007AFF !important;
        {% when 'android' %}
          color: #6200EA !important;
        {% when 'dark' %}
          color: #FFFFFF !important;
        {% when 'gradient' %}
          color: #FFFFFF !important;
        {% when 'custom' %}
          color: {{ section.settings.mobile_nav_text_color | default: '#000000' }} !important;
        {% else %}
          color: rgb(var(--color-foreground)) !important;
      {% endcase %}
    }
    
    .mobile-nav__link.active,
    .mobile-nav__link:hover {
      {% case section.settings.mobile_nav_style %}
        {% when 'ios' %}
          color: #007AFF !important;
          background: rgba(0, 122, 255, 0.1) !important;
        {% when 'android' %}
          color: #6200EA !important;
          background: rgba(98, 0, 234, 0.1) !important;
        {% when 'dark' %}
          color: #60A5FA !important;
          background: rgba(96, 165, 250, 0.1) !important;
        {% when 'gradient' %}
          color: #FFFFFF !important;
          background: rgba(255, 255, 255, 0.2) !important;
        {% when 'custom' %}
          color: {{ section.settings.mobile_nav_active_color | default: '#007AFF' }} !important;
          background: {{ section.settings.mobile_nav_active_color | default: '#007AFF' | color_modify: 'alpha', 0.15 }} !important;
        {% else %}
          color: rgb(var(--color-accent)) !important;
          background: rgba(var(--color-accent), 0.1) !important;
      {% endcase %}
      border-radius: {{ section.settings.mobile_nav_border_radius | default: 8 | divided_by: 2 }}px !important;
    }
    
    .mobile-nav__badge {
      {% case section.settings.mobile_nav_style %}
        {% when 'custom' %}
          background: {{ section.settings.mobile_nav_cart_badge_color | default: '#FF3B30' }} !important;
          color: #FFFFFF !important;
        {% else %}
          background: #FF3B30 !important;
          color: #FFFFFF !important;
      {% endcase %}
    }
    
    {% unless section.settings.mobile_nav_show_labels %}
      .mobile-nav__text {
        display: none !important;
      }
      .mobile-nav__link {
        padding: 8px !important;
        min-height: 40px !important;
      }
    {% endunless %}
    
    /* Show mobile navigation only on mobile devices */
    @media screen and (max-width: 749px) {
      .mobile-nav {
        display: block !important;
      }
    }
    
    /* Ensure mobile navigation is hidden on desktop and tablet */
    @media screen and (min-width: 750px) {
      .mobile-nav {
        display: none !important;
      }
    }
      
    /* Hide header search icon when mobile navigation is enabled */
    {% if section.settings.enable_mobile_navigation %}
      @media screen and (max-width: 749px) {
        .header__search--mobile-only {
          display: none !important;
        }
        
        /* Ensure no duplicate search icons in header on mobile */
        .header__icon--search {
          display: none !important;
        }
        
        /* Hide ALL cart elements in header on mobile to prevent duplicates */
        .header__icon--cart,
        .header__cart,
        .header__cart--desktop,
        #cart-icon-bubble,
        .cart-count-bubble {
          display: none !important;
        }
        
        /* Hide account icon in header on mobile */
        .header__icon--account {
          display: none !important;
        }
        
        /* Ensure proper spacing for content */
        body {
          padding-bottom: 80px !important;
        }
      }
    {% endif %}
  {% endif %}
{%- endstyle -%}

<script src="{{ 'details-disclosure.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'details-modal.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'cart-notification.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'search-form.js' | asset_url }}" defer="defer"></script>
{%- if settings.cart_type == "drawer" -%}
  <script src="{{ 'cart-drawer.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

<svg xmlns="http://www.w3.org/2000/svg" class="hidden">
  <symbol id="icon-search" viewbox="0 0 18 19" fill="none">
    <path fill-rule="evenodd" clip-rule="evenodd" d="M11.03 11.68A5.784 5.784 0 112.85 3.5a5.784 5.784 0 018.18 8.18zm.26 1.12a6.78 6.78 0 11.72-.7l5.4 5.4a.5.5 0 11-.71.7l-5.41-5.4z" fill="currentColor"/>
  </symbol>

  <symbol id="icon-reset" class="icon icon-close"  fill="none" viewBox="0 0 18 18" stroke="currentColor">
    <circle r="8.5" cy="9" cx="9" stroke-opacity="0.2"/>
    <path d="M6.82972 6.82915L1.17193 1.17097" stroke-linecap="round" stroke-linejoin="round" transform="translate(5 5)"/>
    <path d="M1.22896 6.88502L6.77288 1.11523" stroke-linecap="round" stroke-linejoin="round" transform="translate(5 5)"/>
  </symbol>

  <symbol id="icon-close" class="icon icon-close" fill="none" viewBox="0 0 18 17">
    <path d="M.865 15.978a.5.5 0 00.707.707l7.433-7.431 7.579 7.282a.501.501 0 00.846-.37.5.5 0 00-.153-.351L9.712 8.546l7.417-7.416a.5.5 0 10-.707-.708L8.991 7.853 1.413.573a.5.5 0 10-.693.72l7.563 7.268-7.418 7.417z" fill="currentColor">
  </symbol>

  <symbol id="icon-account" viewBox="0 0 18 19" fill="none">
    <path fill-rule="evenodd" clip-rule="evenodd" d="M6 4.5a3 3 0 116 0 3 3 0 01-6 0zm3-4a4 4 0 100 8 4 4 0 000-8zm5.58 12.15c1.12.82 1.83 2.24 1.91 4.85H1.51c.08-2.6.79-4.03 1.9-4.85C4.66 11.75 6.5 11.5 9 11.5s4.35.26 5.58 1.15zM9 10.5c-2.5 0-4.65.24-6.17 1.35C1.27 12.98.5 14.93.5 18v.5h17V18c0-3.07-.77-5.02-2.33-6.15-1.52-1.1-3.67-1.35-6.17-1.35z" fill="currentColor"/>
  </symbol>

  <symbol id="icon-cart" class="icon icon-cart" fill="none" viewBox="0 0 18 19">
    <path fill-rule="evenodd" clip-rule="evenodd" d="M1 1.5A.5.5 0 011.5 1h1.379c.484 0 .962.268 1.196.716l.348.661C4.692 2.808 5.17 3.076 5.654 3.076H16.5a.5.5 0 01.487.613l-1.5 8A.5.5 0 0115 12H6.146l-.348.661A1.5 1.5 0 004.602 13.5H3.5a.5.5 0 010-1h1.102a.5.5 0 00.399-.197l.348-.661c.234-.448.712-.716 1.196-.716H14.37l1.25-7H5.654a1.5 1.5 0 01-1.196-.716l-.348-.661A.5.5 0 013.879 2H1.5a.5.5 0 01-.5-.5z" fill="currentColor"/>
    <path fill-rule="evenodd" clip-rule="evenodd" d="M6 16.5a1.5 1.5 0 113 0 1.5 1.5 0 01-3 0zM7.5 15a1.5 1.5 0 100 3 1.5 1.5 0 000-3z" fill="currentColor"/>
    <path fill-rule="evenodd" clip-rule="evenodd" d="M13 16.5a1.5 1.5 0 113 0 1.5 1.5 0 01-3 0zM14.5 15a1.5 1.5 0 100 3 1.5 1.5 0 000-3z" fill="currentColor"/>
  </symbol>

  <symbol id="icon-cart-empty" class="icon icon-cart-empty" fill="none" viewBox="0 0 18 19">
    <path fill-rule="evenodd" clip-rule="evenodd" d="M1 1.5A.5.5 0 011.5 1h1.379c.484 0 .962.268 1.196.716l.348.661C4.692 2.808 5.17 3.076 5.654 3.076H16.5a.5.5 0 01.487.613l-1.5 8A.5.5 0 0115 12H6.146l-.348.661A1.5 1.5 0 004.602 13.5H3.5a.5.5 0 010-1h1.102a.5.5 0 00.399-.197l.348-.661c.234-.448.712-.716 1.196-.716H14.37l1.25-7H5.654a1.5 1.5 0 01-1.196-.716l-.348-.661A.5.5 0 013.879 2H1.5a.5.5 0 01-.5-.5z" fill="currentColor"/>
  </symbol>
</svg>

{%- liquid
  for block in section.blocks
    if block.type == '@app'
      assign has_app_block = true
    endif
  endfor
-%}

<{% if section.settings.sticky_header_type != 'none' %}sticky-header data-sticky-type="{{ section.settings.sticky_header_type }}"{% else %}div{% endif %} class="header-wrapper color-{{ section.settings.color_scheme }} gradient{% if section.settings.show_line_separator %} header-wrapper--border-bottom{% endif %}">
  
  {%- comment -%} Top Navigation Bar {%- endcomment -%}
  {%- if section.settings.show_top_menu and section.settings.menu != blank -%}
    <div class="top-nav-wrapper page-width">
      <nav class="top-nav" role="navigation" aria-label="Top navigation">
        <ul class="top-nav__list list-unstyled" role="list">
          {%- for link in linklists[section.settings.menu].links -%}
            <li class="top-nav__item">
              <a href="{{ link.url }}" class="top-nav__link link link--text{% if link.current %} top-nav__link--active{% endif %}">
                {{ link.title | escape }}
              </a>
            </li>
          {%- endfor -%}
        </ul>
      </nav>
    </div>
  {%- endif -%}

  {%- liquid
    assign social_links = false
    assign localization_forms = false

    if settings.social_twitter_link != blank or settings.social_facebook_link != blank or settings.social_pinterest_link != blank or settings.social_instagram_link != blank or settings.social_youtube_link != blank or settings.social_vimeo_link != blank or settings.social_tiktok_link != blank or settings.social_tumblr_link != blank or settings.social_snapchat_link != blank
      assign social_links = true
    endif

    if localization.available_countries.size > 1 and section.settings.enable_country_selector or section.settings.enable_language_selector and localization.available_languages.size > 1
      assign localization_forms = true
    endif
  -%}
  <header class="header header--{{ section.settings.logo_position }} header--mobile-{{ section.settings.mobile_logo_position }} page-width{% if section.settings.menu_type_desktop == 'drawer' %} drawer-menu{% endif %}{% if section.settings.menu != blank %} header--has-menu{% endif %}{% if has_app_block %} header--has-app{% endif %}{% if social_links %} header--has-social{% endif %}{% if shop.customer_accounts_enabled %} header--has-account{% endif %}{% if localization_forms %} header--has-localizations{% endif %}">
    
    {%- comment -%} Mobile Menu Drawer {%- endcomment -%}
    {%- if section.settings.menu != blank -%}
      {% render 'header-drawer' %}
    {%- endif -%}

    {%- comment -%} Main Header Row {%- endcomment -%}
    <div class="header__main-row">
      
      {%- comment -%} Left Section {%- endcomment -%}
      <div class="header__left-section">
        {%- comment -%} Search in left position {%- endcomment -%}
        {%- if section.settings.show_search_bar and section.settings.search_position == 'left' -%}
          <div class="header__search-bar header__search-bar--left">
            {% render 'header-search-bar', input_id: 'Search-In-Header-Left' %}
          </div>
        {%- endif -%}
        
        {%- comment -%} Logo {%- endcomment -%}
        {%- if request.page_type == 'index' -%}
          <h1 class="header__heading">
        {%- endif -%}
            <a href="{{ routes.root_url }}" class="header__heading-link link link--text focus-inset">
              {%- if settings.logo != blank -%}
                <div class="header__heading-logo-wrapper">
                  {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
                  {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
                  {% capture sizes %}(max-width: {{ settings.logo_width | times: 2 }}px) 50vw, {{ settings.logo_width }}px{% endcapture %}
                  {% capture widths %}{{ settings.logo_width }}, {{ settings.logo_width | times: 1.5 | round }}, {{ settings.logo_width | times: 2 }}{% endcapture %}
                  {{ settings.logo | image_url: width: 600 | image_tag:
                    class: 'header__heading-logo motion-reduce',
                    widths: widths,
                    height: logo_height,
                    width: settings.logo_width,
                    alt: logo_alt,
                    sizes: sizes,
                    preload: true
                  }}
                </div>
              {%- else -%}
                <span class="h2">{{ shop.name }}</span>
              {%- endif -%}
            </a>
        {%- if request.page_type == 'index' -%}
          </h1>
        {%- endif -%}
      </div>

      {%- comment -%} Center Section {%- endcomment -%}
      <div class="header__center-section">
        {%- comment -%} Search in center position {%- endcomment -%}
        {%- if section.settings.show_search_bar and section.settings.search_position == 'center' -%}
          <div class="header__search-bar header__search-bar--center">
            {% render 'header-search-bar', input_id: 'Search-In-Header-Center' %}
          </div>
        {%- endif -%}
      </div>

      {%- comment -%} Right Section - Account, Cart, Language {%- endcomment -%}
      <div class="header__right-section">
        
        {%- comment -%} Search in right position {%- endcomment -%}
        {%- if section.settings.show_search_bar and section.settings.search_position == 'right' -%}
          <div class="header__search-bar header__search-bar--right">
            {% render 'header-search-bar', input_id: 'Search-In-Header-Right' %}
          </div>
        {%- endif -%}
        
        {%- comment -%} Search icon fallback or mobile {%- endcomment -%}
        {%- if section.settings.show_search_icon and section.settings.show_search_bar == false -%}
          <details-modal class="header__search">
            <details>
              <summary class="header__icon header__icon--search header__icon--summary link focus-inset modal__toggle" aria-haspopup="dialog" aria-label="{{ 'general.search.search' | t }}">
                <span>
                  <svg class="modal__toggle-open icon icon-search" aria-hidden="true" focusable="false">
                    <use href="#icon-search">
                  </svg>
                  <svg class="modal__toggle-close icon icon-close" aria-hidden="true" focusable="false">
                    <use href="#icon-close">
                  </svg>
                </span>
              </summary>
              <div class="search-modal modal__content gradient" role="dialog" aria-modal="true" aria-label="{{ 'general.search.search' | t }}">
                <div class="modal-overlay"></div>
                <div class="search-modal__content search-modal__content-bottom" tabindex="-1">
                  {% render 'header-search', input_id: 'Search-In-Modal' %}
                </div>
              </div>
            </details>
          </details-modal>
        {%- elsif section.settings.show_search_bar -%}
          {%- comment -%} Mobile-only search icon when search bar is enabled {%- endcomment -%}
          <details-modal class="header__search header__search--mobile-only">
            <details>
              <summary class="header__icon header__icon--search header__icon--summary link focus-inset modal__toggle" aria-haspopup="dialog" aria-label="{{ 'general.search.search' | t }}">
                <span>
                  <svg class="modal__toggle-open icon icon-search" aria-hidden="true" focusable="false">
                    <use href="#icon-search">
                  </svg>
                  <svg class="modal__toggle-close icon icon-close" aria-hidden="true" focusable="false">
                    <use href="#icon-close">
                  </svg>
                </span>
              </summary>
              <div class="search-modal modal__content gradient" role="dialog" aria-modal="true" aria-label="{{ 'general.search.search' | t }}">
                <div class="modal-overlay"></div>
                <div class="search-modal__content search-modal__content-bottom" tabindex="-1">
                  {% render 'header-search', input_id: 'Search-In-Mobile-Modal' %}
                </div>
              </div>
            </details>
          </details-modal>
        {%- endif -%}
        
        {%- comment -%} Language Selector {%- endcomment -%}
        {%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
          <div class="header__localization">
            <localization-form class="small-hide medium-hide no-js-hidden" data-prevent-hide>
              {%- form 'localization', id: 'HeaderLanguageForm', class: 'localization-form' -%}
                <div>
                  <h2 class="visually-hidden" id="HeaderLanguageLabel">{{ 'localization.language_label' | t }}</h2>
                  {%- render 'language-localization', localPosition: 'HeaderLanguage' -%}
                </div>
              {%- endform -%}
            </localization-form>
          </div>
        {%- endif -%}

        {%- comment -%} Country Selector {%- endcomment -%}
        {%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
          <div class="header__localization">
            <localization-form class="small-hide medium-hide no-js-hidden" data-prevent-hide>
              {%- form 'localization', id: 'HeaderCountryForm', class: 'localization-form' -%}
                <div>
                  <h2 class="visually-hidden" id="HeaderCountryLabel">{{ 'localization.country_label' | t }}</h2>
                  {%- render 'country-localization', localPosition: 'HeaderCountry' -%}
                </div>
              {%- endform -%}
            </localization-form>
          </div>
        {%- endif -%}

        {%- comment -%} Account Link {%- endcomment -%}
        {%- if shop.customer_accounts_enabled -%}
          <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}" class="header__icon header__icon--account link focus-inset{% if section.settings.menu != blank %} small-hide{% endif %}">
            {%- if section.settings.enable_customer_avatar -%}
              <account-icon>
                {%- if customer and customer.has_avatar? -%}
                  {{ customer | avatar }}
                {%- else -%}
                  <svg class="icon icon-account" aria-hidden="true" focusable="false">
                    <use href="#icon-account">
                  </svg>
                {%- endif -%}
              </account-icon>
            {%- else -%}
              <svg class="icon icon-account" aria-hidden="true" focusable="false">
                <use href="#icon-account">
              </svg>
            {%- endif -%}
            <span class="visually-hidden">
              {%- liquid
                if customer
                  echo 'customer.account_fallback' | t
                else
                  echo 'customer.log_in' | t
                endif
              -%}
            </span>
          </a>
        {%- endif -%}

        {%- comment -%} Cart Link {%- endcomment -%}
        <a href="{{ routes.cart_url }}" class="header__icon header__icon--cart link focus-inset" id="cart-icon-bubble">
          {%- liquid
            if cart == empty
              render 'icon-cart-empty'
            else
              render 'icon-cart'
            endif
          -%}
          <span class="visually-hidden">{{ 'templates.cart.cart' | t }}</span>
          {%- if cart != empty -%}
            <div class="cart-count-bubble">
              {%- if cart.item_count < 100 -%}
                <span aria-hidden="true">{{ cart.item_count }}</span>
              {%- endif -%}
              <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
            </div>
          {%- endif -%}
        </a>

        {%- comment -%} App Blocks {%- endcomment -%}
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when '@app' -%}
              {% render block %}
          {%- endcase -%}
        {%- endfor -%}
      </div>
    </div>
  </header>
</{% if section.settings.sticky_header_type != 'none' %}sticky-header{% else %}div{% endif %}>

{%- if settings.cart_type == "notification" -%}
  {%- render 'cart-notification', color_scheme: section.settings.color_scheme, desktop_menu_type: section.settings.menu_type_desktop -%}
{%- endif -%}

{%- comment -%} Mobile Bottom Navigation - FORCE RENDER {%- endcomment -%}
{% render 'mobile-navigation' %}

{%- comment -%} Emergency Mobile Navigation Fallback {%- endcomment -%}
{% render 'emergency-mobile-navigation' %}

{% javascript %}
  class StickyHeader extends HTMLElement {
    constructor() {
      super();
    }

    connectedCallback() {
      this.header = document.querySelector('.section-header');
      this.headerIsAlwaysSticky = this.getAttribute('data-sticky-type') === 'always' || this.getAttribute('data-sticky-type') === 'reduce-logo-size';
      this.headerBounds = {};

      this.setHeaderHeight();

      window.matchMedia('(max-width: 990px)').addEventListener('change', this.setHeaderHeight.bind(this));

      if (this.headerIsAlwaysSticky) {
        this.header.classList.add('shopify-section-header-sticky');
      };

      this.currentScrollTop = 0;
      this.preventReveal = false;
      this.predictiveSearch = this.querySelector('predictive-search');

      this.onScrollHandler = this.onScroll.bind(this);
      this.hideHeaderOnScrollUp = () => this.preventReveal = true;

      this.addEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.addEventListener('scroll', this.onScrollHandler, false);

      this.createObserver();
    }

    setHeaderHeight() {
      document.documentElement.style.setProperty('--header-height', `${this.header.offsetHeight}px`);
    }

    disconnectedCallback() {
      this.removeEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.removeEventListener('scroll', this.onScrollHandler);
    }

    createObserver() {
      let observer = new IntersectionObserver((entries, observer) => {
        this.headerBounds = entries[0].intersectionRect;
        observer.disconnect();
      });

      observer.observe(this.header);
    }

    onScroll() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      if (this.predictiveSearch && this.predictiveSearch.isOpen) return;

      if (scrollTop > this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (this.preventHide) return;
        requestAnimationFrame(this.hide.bind(this));
      } else if (scrollTop < this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (!this.preventReveal) {
          requestAnimationFrame(this.reveal.bind(this));
        } else {
          window.clearTimeout(this.isScrolling);

          this.isScrolling = setTimeout(() => {
            this.preventReveal = false;
          }, 66);

          requestAnimationFrame(this.hide.bind(this));
        }
      } else if (scrollTop <= this.headerBounds.top) {
        this.header.classList.remove('scrolled-past-header');
        requestAnimationFrame(this.reset.bind(this));
      }

      this.currentScrollTop = scrollTop;
    }

    hide() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-hidden', 'shopify-section-header-sticky');
      this.closeMenuDisclosure();
      this.closeSearchModal();
    }

    reveal() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-sticky', 'animate');
      this.header.classList.remove('shopify-section-header-hidden');
    }

    reset() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.remove('shopify-section-header-hidden', 'shopify-section-header-sticky', 'animate');
    }

    closeMenuDisclosure() {
      this.disclosures = this.disclosures || this.header.querySelectorAll('header-menu');
      this.disclosures.forEach(disclosure => disclosure.close());
    }

    closeSearchModal() {
      this.searchModal = this.searchModal || this.header.querySelector('details-modal');
      this.searchModal.close(false);
    }
  }

  customElements.define('sticky-header', StickyHeader);
{% endjavascript %}

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if settings.logo %}
      "logo": {{ settings.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_twitter_link | json }},
      {{ settings.social_facebook_link | json }},
      {{ settings.social_pinterest_link | json }},
      {{ settings.social_instagram_link | json }},
      {{ settings.social_tiktok_link | json }},
      {{ settings.social_tumblr_link | json }},
      {{ settings.social_snapchat_link | json }},
      {{ settings.social_youtube_link | json }},
      {{ settings.social_vimeo_link | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>

{%- if request.page_type == 'index' -%}
  {% assign potential_action_target = request.origin | append: routes.search_url | append: "?q={search_term_string}" %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "max_blocks": 3,
  "settings": [
    {
      "type": "select",
      "id": "logo_position",
      "options": [
        {
          "value": "top-left",
          "label": "t:sections.header.settings.logo_position.options__2.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.header.settings.logo_position.options__3.label"
        },
        {
          "value": "middle-left",
          "label": "t:sections.header.settings.logo_position.options__1.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.header.settings.logo_position.options__4.label"
        }
      ],
      "default": "middle-left",
      "label": "t:sections.header.settings.logo_position.label",
      "info": "t:sections.header.settings.logo_help.content"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },
    {
      "type": "select",
      "id": "menu_type_desktop",
      "options": [
        {
          "value": "dropdown",
          "label": "t:sections.header.settings.menu_type_desktop.options__1.label"
        },
        {
          "value": "mega",
          "label": "t:sections.header.settings.menu_type_desktop.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.header.settings.menu_type_desktop.options__3.label"
        }
      ],
      "default": "dropdown",
      "label": "t:sections.header.settings.menu_type_desktop.label",
      "info": "t:sections.header.settings.menu_type_desktop.info"
    },
    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        {
          "value": "none",
          "label": "t:sections.header.settings.sticky_header_type.options__1.label"
        },
        {
          "value": "on-scroll-up",
          "label": "t:sections.header.settings.sticky_header_type.options__2.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_header_type.options__3.label"
        },
        {
          "value": "reduce-logo-size",
          "label": "t:sections.header.settings.sticky_header_type.options__4.label"
        }
      ],
      "default": "on-scroll-up",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__1.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "menu_color_scheme",
      "label": "t:sections.header.settings.menu_color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__3.content",
      "info": "t:sections.header.settings.header__4.info"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": false,
      "label": "t:sections.header.settings.enable_country_selector.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__5.content",
      "info": "t:sections.header.settings.header__6.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": false,
      "label": "t:sections.header.settings.enable_language_selector.label"
    },
    {
      "type": "header",
      "content": "🔍 Search Bar Settings"
    },
    {
      "type": "checkbox",
      "id": "show_search_bar",
      "default": true,
      "label": "Show search bar",
      "info": "Display a full search bar in the header center. When enabled, search icon only appears on mobile."
    },
    {
      "type": "checkbox",
      "id": "show_search_icon",
      "default": false,
      "label": "Show search icon only",
      "info": "Display only a search icon (opens modal when clicked). Use this instead of the search bar for a minimal design."
    },
    {
      "type": "text",
      "id": "search_placeholder",
      "default": "Search products...",
      "label": "🔤 Search placeholder text",
      "info": "Custom text shown inside the search bar when empty"
    },
    {
      "type": "select",
      "id": "search_position",
      "options": [
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "left",
          "label": "Left side"
        },
        {
          "value": "right",
          "label": "Right side"
        }
      ],
      "default": "center",
      "label": "📍 Search bar position"
    },
    {
      "type": "color",
      "id": "search_background",
      "label": "🎨 Search background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "search_border",
      "label": "🖌️ Search border color",
      "default": "#e0e0e0"
    },
    {
      "type": "color",
      "id": "search_text",
      "label": "📝 Search text color",
      "default": "#333333"
    },
    {
      "type": "range",
      "id": "search_width",
      "min": 200,
      "max": 600,
      "step": 20,
      "unit": "px",
      "label": "📐 Search bar width",
      "default": 400
    },
    {
      "type": "header",
      "content": "📍 Top Navigation"
    },
    {
      "type": "checkbox",
      "id": "show_top_menu",
      "default": true,
      "label": "Show navigation at top",
      "info": "Display the main navigation menu above the header"
    },
    {
      "type": "header",
      "content": "📱 Mobile Navigation Settings"
    },
    {
      "type": "checkbox",
      "id": "enable_mobile_navigation",
      "default": true,
      "label": "Enable Mobile Bottom Navigation",
      "info": "Show a bottom navigation bar on mobile devices with Home, Search, Cart, and Account links"
    },
    {
      "type": "select",
      "id": "mobile_nav_style",
      "options": [
        {
          "value": "default",
          "label": "Default Theme Style"
        },
        {
          "value": "ios",
          "label": "iOS Style (Blurred)"
        },
        {
          "value": "android",
          "label": "Android Material"
        },
        {
          "value": "dark",
          "label": "Dark Theme"
        },
        {
          "value": "gradient",
          "label": "Colorful Gradient"
        },
        {
          "value": "custom",
          "label": "Custom Colors"
        }
      ],
      "default": "default",
      "label": "Navigation Style",
      "info": "Choose a pre-built style or use custom colors"
    },
    {
      "type": "select",
      "id": "mobile_nav_height",
      "options": [
        {
          "value": "compact",
          "label": "Compact (40px)"
        },
        {
          "value": "normal",
          "label": "Normal (56px)"
        },
        {
          "value": "tall",
          "label": "Tall (72px)"
        }
      ],
      "default": "normal",
      "label": "Navigation Height",
      "info": "Choose the size of the bottom navigation bar"
    },
    {
      "type": "checkbox",
      "id": "mobile_nav_show_labels",
      "default": true,
      "label": "Show Text Labels",
      "info": "Display text under icons (disable for icon-only navigation)"
    },
    {
      "type": "color",
      "id": "mobile_nav_background_color",
      "label": "Background Color",
      "default": "#FFFFFF",
      "info": "Only applies when 'Custom Colors' style is selected"
    },
    {
      "type": "color",
      "id": "mobile_nav_text_color",
      "label": "Icon & Text Color",
      "default": "#000000",
      "info": "Only applies when 'Custom Colors' style is selected"
    },
    {
      "type": "color",
      "id": "mobile_nav_active_color",
      "label": "Active Icon Color",
      "default": "#007AFF",
      "info": "Color for the current page highlight"
    },
    {
      "type": "color",
      "id": "mobile_nav_cart_badge_color",
      "label": "Cart Badge Color",
      "default": "#FF3B30",
      "info": "Color for the cart item count bubble"
    },
    {
      "type": "range",
      "id": "mobile_nav_border_radius",
      "min": 0,
      "max": 30,
      "step": 2,
      "unit": "px",
      "label": "Border Radius",
      "default": 0,
      "info": "Rounded corners for the navigation bar"
    },
    {
      "type": "range",
      "id": "mobile_nav_transparency",
      "min": 70,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Background Opacity",
      "default": 95,
      "info": "Transparency level of the background"
    },
    {
      "type": "header",
      "content": "Navigation Items"
    },
    {
      "type": "checkbox",
      "id": "mobile_nav_show_home",
      "default": true,
      "label": "Show Home Button",
      "info": "Display home icon in mobile navigation"
    },
    {
      "type": "checkbox",
      "id": "mobile_nav_show_search",
      "default": true,
      "label": "Show Search Button",
      "info": "Display search icon in mobile navigation"
    },
    {
      "type": "checkbox",
      "id": "mobile_nav_show_cart",
      "default": true,
      "label": "Show Cart Button",
      "info": "Display cart icon with item count in mobile navigation"
    },
    {
      "type": "checkbox",
      "id": "mobile_nav_show_account",
      "default": true,
      "label": "Show Account Button",
      "info": "Display account/login icon in mobile navigation"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.mobile_layout.content"
    },
    {
      "type": "select",
      "id": "mobile_logo_position",
      "options": [
        {
          "value": "center",
          "label": "t:sections.header.settings.mobile_logo_position.options__1.label"
        },
        {
          "value": "left",
          "label": "t:sections.header.settings.mobile_logo_position.options__2.label"
        }
      ],
      "default": "center",
      "label": "t:sections.header.settings.mobile_logo_position.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.header.settings.margin_bottom.label",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 20
    }
  ],
  "blocks": [
    {
      "type": "@app"
    }
  ]
}
{% endschema %}
